{
  "name": "stdout",
  "category": "outputs",
  "status": "stable",
  "summary": "Print each message to stdout (one line per message).",
  "configSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "codec": {
        "type": "string",
        "default": "lines",
        "enum": ["lines", "all-bytes", "delim"]
      }
    }
  }
}
