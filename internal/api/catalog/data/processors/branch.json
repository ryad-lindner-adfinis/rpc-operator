{
  "name": "branch",
  "category": "processors",
  "status": "stable",
  "summary": "Routes messages through a sub-pipeline with optional request/result mapping.",
  "configSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
      "request_map": {
        "type": "string",
        "description": "Bloblang mapping applied to each message before the sub-pipeline. Empty = copy original."
      },
      "result_map": {
        "type": "string",
        "description": "Bloblang mapping merging sub-pipeline results back into the original. Empty = keep original."
      }
    },
    "additionalProperties": false
  }
}
